# Configuration

All settings use the `LLM_` prefix and are read from environment variables or `.env`.

See the [README](../README.md) for the full configuration table.

## GatewayConfig API

::: llm_gateway.config.GatewayConfig
